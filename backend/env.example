# Database
DATABASE_URL="postgresql://user:password@localhost:5432/fluencyon?schema=public"

# JWT (production: use a strong 32+ char random string, e.g. openssl rand -base64 32)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV=development

# CORS (production only): comma-separated origins, e.g. https://yourapp.com,https://www.yourapp.com
# ALLOWED_ORIGINS="https://yourapp.com"

# Frontend URL for email links (e.g. setup profile). Default in dev: http://localhost:5173
# FRONTEND_URL="http://localhost:5173"

# Email – Resend (student setup invitation). Without RESEND_API_KEY, link is only logged to console.
# Get API key at https://resend.com. From address must be a verified domain or onboarding@resend.dev.
# RESEND_API_KEY="re_xxxx"
# RESEND_FROM="FluencyOn <noreply@yourdomain.com>"

# File Storage – Cloudflare R2 (recommended: cheap, no egress fees)
# When set, uploads go to R2 instead of local disk. Max file size becomes 5GB (for videos).
# Get these from Cloudflare Dashboard → R2 → API Tokens; create a bucket and enable public access for R2_PUBLIC_URL.
# R2_ACCOUNT_ID=""
# R2_ACCESS_KEY_ID=""
# R2_SECRET_ACCESS_KEY=""
# R2_BUCKET_NAME=""
# R2_PUBLIC_URL=""
# Optional: AWS S3 (not wired yet; use R2 for lower cost)
# AWS_S3_BUCKET_NAME=""
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION="us-east-1"

